// Отрисовка холста
let update = { id: null, active: false };

// Pause
let pause = { keyCode: 81, active: false };

// Параметры холста
let settingsCanvas = 
{
  // Ширина и высота изменяется в клетках!
  w: 80, 
  h: 40,
  byDefauld: {w: 80, h: 39}
};
