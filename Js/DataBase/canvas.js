// Отрисовка холста
let update = { id: null, active: false };

// Pause
let pause = { keyCode: 81, active: false };

// Параметры холста
let settingsCanvas = 
{
  // Ширина и высота изменяется в клетках!
  w: 64, 
  h: 31,
  byDefauld: {w: 64, h: 31}
};

// Сохранение/экспорт/импорт активированы
let saveExpImp = 
{
  active: false
};
